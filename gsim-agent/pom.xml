<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.gsim</groupId>
        <artifactId>GSimulator</artifactId>
        <version>0.1.0-Alpha260723</version>
    </parent>

    <artifactId>gsim-agent</artifactId>
    <packaging>jar</packaging>

    <name>GSim Agent</name>
    <description>子 Agent 服务 — Agent 运行时（ToolLoop/Orchestrator/SubAgent）+ 全部工具实现 + MCP 组装 + core/map 工具桥接注册层</description>

    <dependencies>
        <dependency>
            <groupId>com.gsim</groupId>
            <artifactId>gsim-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.gsim</groupId>
            <artifactId>gsim-agentlib</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 地图工具（com.gsim.agent.tools.map.*）依赖 gsim-map 业务层（MapService/MapData 等） -->
        <dependency>
            <groupId>com.gsim</groupId>
            <artifactId>gsim-map</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <!-- 与兄弟模块一致的注解依赖；防御性补齐 -->
        <dependency>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-annotations</artifactId>
            <version>4.10.2</version>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jline</groupId>
            <artifactId>jline</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- 测试用（TestLogAppender 基于 log4j2） -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- SpotBugs: ratchet 模式 — baseline 文件 config/spotbugs/gsim-agent-baseline.xml（2026-08-13 重建） -->
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <configuration>
                    <effort>Max</effort>
                    <threshold>Medium</threshold>
                    <failOnError>false</failOnError>
                    <includeTests>false</includeTests>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
