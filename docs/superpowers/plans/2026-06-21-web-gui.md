# GSimulator Web GUI — 实现计划

> **For agentic workers:** 使用 superpowers:subagent-driven-development 实现此计划，逐任务推进。

**目标:** 在现有 Java 21 + Maven 项目中实现 HTMX + SSE + Mermaid Web GUI。

**设计文档:** docs/superpowers/specs/2026-06-21-web-gui-design.md

## 全局约束

- 零 npm/Node 依赖：所有前端库通过 CDN 加载
- Thymeleaf 3.1.3.RELEASE
- 模板: src/main/resources/webui/templates/
- 静态资源: src/main/resources/webui/static/
- Java 新类: src/main/java/com/gsim/webui/
- WebUI 独立端口 8711
- 复用现有 ApplicationContext、EventBus、TaskManager、DataManager
- 暗色主题：gray-900 背景，green-400 终端绿强调
- 响应式断点：桌面 >=768px，移动 <768px

---

## Phase 1: 骨架搭建 — WebUiServer + Thymeleaf + 主页面

### Task 1.1: 添加 Thymeleaf 依赖

**文件:**
- 修改: `pom.xml`

- [ ] **Step 1: 在 pom.xml 中添加 thymeleaf 依赖**

在 `pom.xml` 的 `<dependencies>` 块末尾（`</dependencies>` 之前）添加：

```xml
<!-- Thymeleaf — HTML 模板引擎 -->
<dependency>
    <groupId>org.thymeleaf</groupId>
    <artifactId>thymeleaf</artifactId>
    <version>3.1.3.RELEASE</version>
</dependency>
```

- [ ] **Step 2: 验证依赖下载**

```bash
mvn dependency:resolve 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
git add pom.xml
git commit -m "chore: add Thymeleaf 3.1.3 dependency for WebUI templates"
```

---

### Task 1.2: WebUiConfig + AppConfig 扩展

**文件:**
- 创建: `src/main/java/com/gsim/webui/WebUiConfig.java`
- 修改: `src/main/java/com/gsim/app/AppConfig.java`

**接口:**
- 产出: `WebUiConfig` record — `host()`, `port()`, `enabled()`, `from(AppConfig)`, `getBaseUrl()`
- 消费: `AppConfig.getWebUiHost()`, `getWebUiPort()`, `isWebUiEnabled()`

- [ ] **Step 1: 创建 WebUiConfig.java**

```java
package com.gsim.webui;

/**
 * WebUI 独立配置。
 */
public record WebUiConfig(
        String host,
        int port,
        boolean enabled
) {
    public static WebUiConfig defaults() {
        return new WebUiConfig("127.0.0.1", 8711, false);
    }

    /**
     * 从 AppConfig 提取 WebUI 配置。
     */
    public static WebUiConfig from(com.gsim.app.AppConfig appConfig) {
        return new WebUiConfig(
                appConfig.getWebUiHost(),
                appConfig.getWebUiPort(),
                appConfig.isWebUiEnabled()
        );
    }

    public String getBaseUrl() {
        return "http://" + host + ":" + port;
    }
}
```

- [ ] **Step 2: 在 AppConfig 中添加 WebUI 配置字段**

在 `AppConfig.java` 的字段区（`private final int compactSummaryMaxChars;` 之后）添加：

```java
    // WebUI 配置
    private final String webUiHost;
    private final int webUiPort;
    private final boolean webUiEnabled;
```

在构造函数末尾（`this.compactSummaryMaxChars = ...` 之后）添加初始化：

```java
        // WebUI 配置
        this.webUiHost = isBlank(result.get("webui.host")) ? "127.0.0.1" : result.get("webui.host");
        this.webUiPort = parseInt(result.get("webui.port"), 8711);
        this.webUiEnabled = parseBoolean(result.get("webui.enabled"), false);
```

在 getter 区（`getCompactSummaryMaxChars()` 之后）添加：

```java
    public String getWebUiHost() { return webUiHost; }
    public int getWebUiPort() { return webUiPort; }
    public boolean isWebUiEnabled() { return webUiEnabled; }
```

- [ ] **Step 3: 编译验证**

```bash
cd /home/cna/GSimulator && mvn compile 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add src/main/java/com/gsim/webui/WebUiConfig.java src/main/java/com/gsim/app/AppConfig.java
git commit -m "feat: add WebUiConfig and AppConfig webUI fields (host/port/enabled)"
```

---

### Task 1.3: WebUiServer 基础骨架

**文件:**
- 创建: `src/main/java/com/gsim/webui/WebUiServer.java`

**接口:**
- 消费: `WebUiConfig` (Task 1.2), `ApplicationContext` (existing)
- 产出: `WebUiServer` — `start()`, `stop()`, `isRunning()`, `forceEnable()`

- [ ] **Step 1: 创建 WebUiServer.java**

```java
package com.gsim.webui;

import com.gsim.app.ApplicationContext;
import com.gsim.webui.handlers.PageHandler;
import com.gsim.webui.handlers.ChatHandler;
import com.gsim.webui.handlers.TimelineHandler;
import com.gsim.webui.handlers.KnowledgeHandler;
import com.gsim.webui.handlers.StaticHandler;
import com.sun.net.httpserver.HttpServer;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.io.IOException;
import java.net.InetSocketAddress;
import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;

/**
 * WebUI 独立 HTTP 服务器，端口 8711。
 */
public class WebUiServer {

    private static final Logger log = LoggerFactory.getLogger(WebUiServer.class);

    private final WebUiConfig config;
    private final ApplicationContext ctx;
    private HttpServer server;
    private ExecutorService executor;
    private boolean forceEnabled = false;

    public WebUiServer(WebUiConfig config, ApplicationContext ctx) {
        this.config = config;
        this.ctx = ctx;
    }

    public void forceEnable() {
        this.forceEnabled = true;
    }

    public void start() throws IOException {
        if (!config.enabled() && !forceEnabled) {
            log.info("WebUI is disabled. Skipping server start.");
            return;
        }

        InetSocketAddress address = new InetSocketAddress(config.host(), config.port());
        server = HttpServer.create(address, 0);

        registerHandlers();

        executor = Executors.newVirtualThreadPerTaskExecutor();
        server.setExecutor(executor);
        server.start();

        log.info("WebUI server started on {}:{}", config.host(), config.port());
        System.out.println("🌐 Web GUI: " + config.getBaseUrl());
    }

    private void registerHandlers() {
        server.createContext("/static", new StaticHandler());
        server.createContext("/", new PageHandler(ctx));
        server.createContext("/chat", new ChatHandler(ctx));
        server.createContext("/timeline", new TimelineHandler(ctx));
        server.createContext("/knowledge", new KnowledgeHandler(ctx));
    }

    public void stop() {
        if (server != null) {
            server.stop(2);
            log.info("WebUI server stopped.");
        }
        if (executor != null && !executor.isShutdown()) {
            executor.shutdownNow();
        }
    }

    public boolean isRunning() {
        return server != null;
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
cd /home/cna/GSimulator && mvn compile 2>&1 | tail -5
```

Expected: COMPILATION ERROR（handler 类尚不存在 — 预期行为，Task 1.4-1.7 会创建）

- [ ] **Step 3: Commit**

```bash
git add src/main/java/com/gsim/webui/WebUiServer.java
git commit -m "feat: add WebUiServer skeleton (port 8711, virtual threads)"
```

---

### Task 1.4: TemplateRenderer — Thymeleaf 封装

**文件:**
- 创建: `src/main/java/com/gsim/webui/TemplateRenderer.java`

**接口:**
- 产出: `TemplateRenderer.render(templateName, variables)` — 静态方法，返回 String

- [ ] **Step 1: 创建 TemplateRenderer.java**

```java
package com.gsim.webui;

import org.thymeleaf.TemplateEngine;
import org.thymeleaf.context.Context;
import org.thymeleaf.templatemode.TemplateMode;
import org.thymeleaf.templateresolver.ClassLoaderTemplateResolver;

import java.util.Locale;
import java.util.Map;

/**
 * Thymeleaf 模板渲染器 — 单例封装。
 *
 * <p>模板从 classpath:/webui/templates/ 加载，后缀 .html。
 */
public class TemplateRenderer {

    private static final TemplateEngine ENGINE = createEngine();

    private static TemplateEngine createEngine() {
        ClassLoaderTemplateResolver resolver = new ClassLoaderTemplateResolver();
        resolver.setPrefix("/webui/templates/");
        resolver.setSuffix(".html");
        resolver.setTemplateMode(TemplateMode.HTML);
        resolver.setCharacterEncoding("UTF-8");
        resolver.setCacheable(false);  // 开发阶段不缓存

        TemplateEngine engine = new TemplateEngine();
        engine.setTemplateResolver(resolver);
        return engine;
    }

    /**
     * 渲染模板。
     *
     * @param templateName 模板名（不含路径前缀和后缀），如 "index"
     * @param variables    模板变量
     * @return 渲染后的 HTML 字符串
     */
    public static String render(String templateName, Map<String, Object> variables) {
        Context ctx = new Context(Locale.getDefault(), variables);
        return ENGINE.process(templateName, ctx);
    }

    /**
     * 无变量渲染。
     */
    public static String render(String templateName) {
        return render(templateName, Map.of());
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
cd /home/cna/GSimulator && mvn compile 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
git add src/main/java/com/gsim/webui/TemplateRenderer.java
git commit -m "feat: add TemplateRenderer — Thymeleaf wrapper for webui templates"
```

---

### Task 1.5: StaticHandler — 静态资源服务

**文件:**
- 创建: `src/main/java/com/gsim/webui/handlers/StaticHandler.java`

- [ ] **Step 1: 创建 StaticHandler.java**

```java
package com.gsim.webui.handlers;

import com.sun.net.httpserver.HttpExchange;
import com.sun.net.httpserver.HttpHandler;

import java.io.IOException;
import java.io.InputStream;
import java.io.OutputStream;

/**
 * 静态资源服务 — 从 classpath:/webui/static/ 读取文件。
 */
public class StaticHandler implements HttpHandler {

    private static final String PREFIX = "/static/";
    private static final String BASE = "/webui/static/";

    private static String mimeType(String path) {
        if (path.endsWith(".css")) return "text/css; charset=utf-8";
        if (path.endsWith(".js")) return "application/javascript; charset=utf-8";
        if (path.endsWith(".svg")) return "image/svg+xml";
        if (path.endsWith(".png")) return "image/png";
        if (path.endsWith(".ico")) return "image/x-icon";
        return "application/octet-stream";
    }

    @Override
    public void handle(HttpExchange exchange) throws IOException {
        String path = exchange.getRequestURI().getPath();
        if (!path.startsWith(PREFIX)) {
            exchange.sendResponseHeaders(404, -1);
            exchange.close();
            return;
        }

        String resourcePath = BASE + path.substring(PREFIX.length());
        if (resourcePath.contains("..")) {
            exchange.sendResponseHeaders(403, -1);
            exchange.close();
            return;
        }

        try (InputStream in = getClass().getResourceAsStream("/" + resourcePath)) {
            if (in == null) {
                exchange.sendResponseHeaders(404, -1);
                exchange.close();
                return;
            }
            byte[] bytes = in.readAllBytes();
            exchange.getResponseHeaders().set("Content-Type", mimeType(path));
            exchange.sendResponseHeaders(200, bytes.length);
            try (OutputStream out = exchange.getResponseBody()) {
                out.write(bytes);
            }
        }
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
cd /home/cna/GSimulator && mvn compile 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
git add src/main/java/com/gsim/webui/handlers/StaticHandler.java
git commit -m "feat: add StaticHandler for serving webui static assets from classpath"
```

---

### Task 1.6: PageHandler + index.html — 主页面

**文件:**
- 创建: `src/main/java/com/gsim/webui/handlers/PageHandler.java`
- 创建: `src/main/resources/webui/templates/index.html`

**接口:**
- 消费: `TemplateRenderer` (Task 1.4), `ApplicationContext` (existing)
- 产出: `PageHandler` — 处理 `/`, `/chat`, `/timeline`, `/knowledge` 的 GET 请求

- [ ] **Step 1: 创建 PageHandler.java**

```java
package com.gsim.webui.handlers;

import com.gsim.app.ApplicationContext;
import com.gsim.webui.TemplateRenderer;
import com.sun.net.httpserver.HttpExchange;
import com.sun.net.httpserver.HttpHandler;

import java.io.IOException;
import java.io.OutputStream;
import java.nio.charset.StandardCharsets;
import java.util.Map;

/**
 * 页面路由处理器。
 *
 * <p>GET / → index.html（完整页面）
 * <p>GET /chat → 对话面板片段
 * <p>GET /timeline → 时间线面板片段
 * <p>GET /knowledge → 知识库面板片段
 */
public class PageHandler implements HttpHandler {

    private final ApplicationContext ctx;

    public PageHandler(ApplicationContext ctx) {
        this.ctx = ctx;
    }

    @Override
    public void handle(HttpExchange exchange) throws IOException {
        String path = exchange.getRequestURI().getPath();
        String method = exchange.getRequestMethod();

        if (!"GET".equalsIgnoreCase(method)) {
            exchange.sendResponseHeaders(405, -1);
            exchange.close();
            return;
        }

        String html;
        String contentType = "text/html; charset=utf-8";

        switch (path) {
            case "/" -> {
                var dm = ctx.getDataManager();
                String activeBranch = dm != null ? dm.getActiveBranch() : null;
                String activeWorld = dm != null ? dm.getActiveWorld() : null;
                html = TemplateRenderer.render("index", Map.of(
                        "activeBranchId", activeBranch != null ? activeBranch : "—",
                        "activeWorld", activeWorld != null ? activeWorld : "—"
                ));
            }
            case "/chat" -> html = TemplateRenderer.render("fragments/chat-panel");
            case "/timeline" -> html = TemplateRenderer.render("fragments/timeline-panel");
            case "/knowledge" -> html = TemplateRenderer.render("fragments/knowledge-panel");
            default -> {
                exchange.sendResponseHeaders(404, -1);
                exchange.close();
                return;
            }
        }

        byte[] bytes = html.getBytes(StandardCharsets.UTF_8);
        exchange.getResponseHeaders().set("Content-Type", contentType);
        exchange.sendResponseHeaders(200, bytes.length);
        try (OutputStream out = exchange.getResponseBody()) {
            out.write(bytes);
        }
    }
}
```

- [ ] **Step 2: 创建 index.html 主模板**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GSimulator — 推演工作台</title>
    <script src="https://unpkg.com/htmx.org@2.0.4"></script>
    <script src="https://unpkg.com/htmx.org@2.0.4/dist/ext/sse.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.min.js"></script>
    <style>
        body { background-color: #111827; color: #e5e7eb; }
        .panel { background-color: #1f2937; border: 1px solid #374151; border-radius: 0.5rem; }
        .terminal-green { color: #34d399; }
        .bg-terminal { background-color: #34d399; }
        .input-area { background-color: #111827; border: 1px solid #4b5563; }
        .input-area:focus { border-color: #34d399; outline: none; }
        .msg-user { background-color: #1e3a5f; }
        .msg-assistant { background-color: #1f2937; }
        .tool-card { background-color: #1a1a2e; border-left: 3px solid #f59e0b; }
        .tool-card.success { border-left-color: #34d399; }
        .tool-card.error { border-left-color: #ef4444; }
        .sidebar-overlay { display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.6); z-index: 40; }
        .sidebar { display: none; position: fixed; top: 0; left: 0; width: 16rem; height: 100%; background: #1f2937; z-index: 50; padding: 1rem; }
        @media (min-width: 768px) {
            .mobile-only { display: none !important; }
            .sidebar-overlay, .sidebar { display: none !important; }
            .desktop-panel { display: block !important; }
        }
        @media (max-width: 767px) {
            .desktop-panel { display: none !important; }
            .mobile-panel.active { display: block !important; }
        }
    </style>
</head>
<body class="min-h-screen flex flex-col" hx-ext="sse">

    <!-- ===== 顶栏 ===== -->
    <header class="flex items-center justify-between px-4 py-2 bg-gray-800 border-b border-gray-700">
        <div class="flex items-center gap-3">
            <button class="mobile-only text-gray-400 hover:text-green-400 text-xl"
                    onclick="toggleSidebar()">&#9776;</button>
            <h1 class="text-lg font-bold terminal-green">GSimulator</h1>
        </div>
        <div class="text-xs text-gray-500">
            <span>世界: <span th:text="${activeWorld}">—</span></span>
            <span class="ml-4">分支: <span th:text="${activeBranchId}">—</span></span>
        </div>
    </header>

    <!-- ===== 移动端侧边栏 ===== -->
    <div id="sidebar-overlay" class="sidebar-overlay" onclick="toggleSidebar()"></div>
    <div id="sidebar" class="sidebar">
        <button class="text-gray-400 hover:text-white mb-6" onclick="toggleSidebar()">&times; 关闭</button>
        <nav class="flex flex-col gap-2">
            <button onclick="switchMobilePanel('chat')"
                    class="text-left px-3 py-2 rounded hover:bg-gray-700 text-gray-300 hover:text-green-400">
                &#x1F4AC; Agent 对话</button>
            <button onclick="switchMobilePanel('timeline')"
                    class="text-left px-3 py-2 rounded hover:bg-gray-700 text-gray-300 hover:text-green-400">
                &#x1F333; 时间线分支</button>
            <button onclick="switchMobilePanel('knowledge')"
                    class="text-left px-3 py-2 rounded hover:bg-gray-700 text-gray-300 hover:text-green-400">
                &#x1F50D; 知识库</button>
        </nav>
    </div>

    <!-- ===== 主内容区 ===== -->
    <main class="flex-1 flex overflow-hidden">
        <!-- 桌面版三栏 -->
        <div class="hidden md:flex w-full gap-2 p-2">
            <div id="desktop-timeline" class="w-1/4 panel p-3 overflow-y-auto">
                <div hx-get="/timeline" hx-trigger="load" hx-swap="innerHTML">
                    <div class="text-gray-500 text-sm">加载中...</div>
                </div>
            </div>
            <div id="desktop-chat" class="w-2/4 panel p-3 flex flex-col">
                <div hx-get="/chat" hx-trigger="load" hx-swap="innerHTML">
                    <div class="text-gray-500 text-sm">加载中...</div>
                </div>
            </div>
            <div id="desktop-knowledge" class="w-1/4 panel p-3 overflow-y-auto">
                <div hx-get="/knowledge" hx-trigger="load" hx-swap="innerHTML">
                    <div class="text-gray-500 text-sm">加载中...</div>
                </div>
            </div>
        </div>

        <!-- 移动版单面板 -->
        <div class="md:hidden w-full p-2">
            <div id="mobile-chat" class="mobile-panel active panel p-3 h-full">
                <div hx-get="/chat" hx-trigger="load" hx-swap="innerHTML"></div>
            </div>
            <div id="mobile-timeline" class="mobile-panel panel p-3 h-full hidden">
                <div hx-get="/timeline" hx-trigger="load" hx-swap="innerHTML"></div>
            </div>
            <div id="mobile-knowledge" class="mobile-panel panel p-3 h-full hidden">
                <div hx-get="/knowledge" hx-trigger="load" hx-swap="innerHTML"></div>
            </div>
        </div>
    </main>

    <script src="/static/app.js"></script>
</body>
</html>
```

- [ ] **Step 3: 编译验证**

```bash
cd /home/cna/GSimulator && mvn compile 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add src/main/java/com/gsim/webui/handlers/PageHandler.java \
        src/main/resources/webui/templates/index.html
git commit -m "feat: add PageHandler + index.html — three-panel layout with responsive breakpoints"
```

---

### Task 1.7: 创建占位片段模板

**文件:**
- 创建: `src/main/resources/webui/templates/fragments/chat-panel.html`
- 创建: `src/main/resources/webui/templates/fragments/timeline-panel.html`
- 创建: `src/main/resources/webui/templates/fragments/knowledge-panel.html`

- [ ] **Step 1: 创建 chat-panel.html（空壳）**

```html
<div class="flex flex-col h-full">
    <div class="text-sm font-bold terminal-green mb-2">&#x1F4AC; Agent 对话</div>
    <div id="chat-context" class="text-xs text-gray-500 mb-2">分支: —</div>
    <div id="chat-messages" class="flex-1 overflow-y-auto mb-2 space-y-2">
        <div class="text-gray-600 text-sm">发送消息开始对话</div>
    </div>
    <form id="chat-form" class="flex gap-2"
          hx-post="/chat/send" hx-swap="none">
        <input type="text" name="message" placeholder="输入消息..."
               class="flex-1 input-area rounded px-3 py-2 text-sm text-gray-200">
        <button type="submit"
                class="px-4 py-2 bg-green-700 hover:bg-green-600 text-white text-sm rounded">
            发送
        </button>
    </form>
</div>
```

- [ ] **Step 2: 创建 timeline-panel.html（空壳）**

```html
<div>
    <div class="text-sm font-bold terminal-green mb-2">&#x1F333; 时间线分支</div>
    <div id="timeline-graph" class="mb-3">
        <div class="text-gray-600 text-sm">加载中...</div>
    </div>
    <div id="timeline-nodes" class="text-xs text-gray-500">
    </div>
</div>
```

- [ ] **Step 3: 创建 knowledge-panel.html（空壳）**

```html
<div>
    <div class="text-sm font-bold terminal-green mb-2">&#x1F50D; 知识库</div>
    <form id="search-form" class="mb-3"
          hx-get="/knowledge/search" hx-target="#search-results" hx-swap="innerHTML">
        <div class="flex gap-1 mb-2">
            <input type="text" name="q" placeholder="搜索关键词..."
                   class="flex-1 input-area rounded px-3 py-1 text-sm text-gray-200">
            <button type="submit"
                    class="px-3 py-1 bg-green-700 hover:bg-green-600 text-white text-sm rounded">
                搜索
            </button>
        </div>
        <div class="flex gap-2 text-xs">
            <label class="text-gray-400">
                <input type="radio" name="mode" value="keyword" checked> 关键词
            </label>
            <label class="text-gray-400">
                <input type="radio" name="mode" value="semantic"> 语义
            </label>
            <input type="hidden" name="topK" value="10">
        </div>
    </form>
    <div id="search-results" class="text-xs text-gray-500">
    </div>
</div>
```

- [ ] **Step 4: Commit**

```bash
git add src/main/resources/webui/templates/fragments/
git commit -m "feat: add placeholder fragment templates for chat/timeline/knowledge panels"
```

---

### Task 1.8: 创建 app.js + 命令行集成

**文件:**
- 创建: `src/main/resources/webui/static/app.js`
- 修改: `src/main/java/com/gsim/config/ConfigLoader.java`
- 修改: `src/main/java/com/gsim/Main.java`
- 修改: `src/main/java/com/gsim/app/GSimulatorApplication.java`

- [ ] **Step 1: 创建 app.js**

```javascript
// GSimulator WebUI — 前端交互脚本

// ---- Mermaid 初始化 ----
mermaid.initialize({
    startOnLoad: false,
    theme: 'dark',
    themeVariables: {
        primaryColor: '#1f2937',
        primaryTextColor: '#e5e7eb',
        primaryBorderColor: '#374151',
        lineColor: '#34d399',
        secondaryColor: '#1a1a2e',
        tertiaryColor: '#111827'
    }
});

// 全局函数：渲染 Mermaid 图表
async function renderMermaid(elementId, code) {
    const el = document.getElementById(elementId);
    if (!el || !code) return;
    try {
        const { svg } = await mermaid.render(elementId + '-svg', code);
        el.innerHTML = svg;
    } catch (e) {
        el.innerHTML = '<div class="text-red-500 text-xs">渲染失败: ' + e.message + '</div>';
    }
}

// ---- 移动端侧边栏 ----
function toggleSidebar() {
    const overlay = document.getElementById('sidebar-overlay');
    const sidebar = document.getElementById('sidebar');
    const isOpen = sidebar.style.display === 'block';
    overlay.style.display = isOpen ? 'none' : 'block';
    sidebar.style.display = isOpen ? 'none' : 'block';
}

function switchMobilePanel(name) {
    document.querySelectorAll('.mobile-panel').forEach(p => p.classList.add('hidden'));
    document.querySelectorAll('.mobile-panel').forEach(p => p.classList.remove('active'));
    const target = document.getElementById('mobile-' + name);
    if (target) {
        target.classList.remove('hidden');
        target.classList.add('active');
    }
    toggleSidebar();
}
```

- [ ] **Step 2: 在 ConfigLoader 中添加 --webui 参数**

修改 `ConfigLoader.java`：

在 `CliArgs` record 中添加 `boolean webui` 字段：
```java
    public record CliArgs(String configPath, boolean initConfig, boolean doctor,
                          boolean noWizard, boolean help, boolean http, boolean cli,
                          boolean webui) {}
```

在 `parseCliArgs()` 的变量声明中添加：
```java
        boolean webui = false;
```

在 switch 块中添加：
```java
                case "--webui":
                    webui = true;
                    break;
```

更新 return 语句：
```java
        return new CliArgs(configPath, initConfig, doctor, noWizard, help, http, cli, webui);
```

- [ ] **Step 3: 在 Main.java 中集成 --webui 参数**

修改 `Main.java`：

提取 webuiMode：
```java
            boolean webuiMode = cliArgs.webui();
```

更新 GSimulatorApplication 构造调用：
```java
            GSimulatorApplication app = new GSimulatorApplication(config, cliMode, httpMode, webuiMode);
```

在 `printUsage()` 中添加：
```java
        System.out.println("  --webui            启动 Web GUI 服务器 (默认 127.0.0.1:8711)");
```

- [ ] **Step 4: 在 GSimulatorApplication 中启动 WebUI**

修改 `GSimulatorApplication.java`：

添加字段：
```java
    private final boolean webuiMode;
    private final com.gsim.webui.WebUiServer webUiServer;
```

修改主构造函数签名，添加 `boolean webuiMode` 参数：
```java
    public GSimulatorApplication(AppConfig config, boolean cliMode, boolean httpMode, boolean webuiMode) {
        this.config = config;
        this.cliMode = cliMode;
        this.httpMode = httpMode;
        this.webuiMode = webuiMode;
        // ... rest of constructor
```

在构造函数末尾（registerCommands 之后）添加：
```java
        // 创建 WebUiServer
        com.gsim.webui.WebUiConfig webUiConfig =
                com.gsim.webui.WebUiConfig.from(config);
        this.webUiServer = new com.gsim.webui.WebUiServer(webUiConfig, ctx);
```

修改旧构造函数为兼容调用：
```java
    public GSimulatorApplication(AppConfig config) {
        this(config, true, false, false);
    }

    public GSimulatorApplication(AppConfig config, boolean cliMode, boolean httpMode) {
        this(config, cliMode, httpMode, false);
    }
```

在 `start()` 方法中，HTTP API 启动之后添加：
```java
        // WebUI 模式：启动 WebUI 服务器
        if (webuiMode) {
            webUiServer.forceEnable();
        }
        if (webuiMode || config.isWebUiEnabled()) {
            webUiServer.start();
        }
```

在 `stop()` 方法中添加：
```java
        if (webUiServer != null) webUiServer.stop();
```

- [ ] **Step 5: 编译验证**

```bash
cd /home/cna/GSimulator && mvn compile 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 6: 启动测试**

```bash
timeout 5 java -cp target/classes:$(mvn -q dependency:build-classpath -Dmdep.outputFile=/dev/stdout) \
  com.gsim.Main --http --webui --no-wizard 2>&1 || true
```

Expected: 输出中包含 `🌐 Web GUI: http://127.0.0.1:8711`

- [ ] **Step 7: Commit**

```bash
git add src/main/resources/webui/static/app.js \
        src/main/java/com/gsim/config/ConfigLoader.java \
        src/main/java/com/gsim/Main.java \
        src/main/java/com/gsim/app/GSimulatorApplication.java
git commit -m "feat: integrate WebUiServer with --webui CLI flag and app.js"
```

---

## Phase 1 验收

```bash
# 编译
mvn compile
# 启动
timeout 5 java -cp target/classes:$(mvn -q dependency:build-classpath -Dmdep.outputFile=/dev/stdout) \
  com.gsim.Main --http --webui --no-wizard 2>&1 || true
```

Expected:
- BUILD SUCCESS
- 输出 `🌐 Web GUI: http://127.0.0.1:8711`
- 浏览器访问 `http://127.0.0.1:8711` 显示三栏布局（桌面）/ 单栏 + 汉堡菜单（移动端）


---

## Phase 2: Agent 对话面板 — SSE 流式 + 消息历史

### Task 2.1: ChatHandler — 对话 API

**文件:**
- 创建: `src/main/java/com/gsim/webui/handlers/ChatHandler.java`

**接口:**
- 消费: `ApplicationContext` (existing), `TaskManager` (existing via ctx.getApiManager()), `EventBus` (existing)
- 产出: `ChatHandler` — 处理 `/chat/send` (POST), `/chat/stream` (GET SSE), `/chat/messages` (GET), `/chat/context` (GET)

- [ ] **Step 1: 创建 ChatHandler.java**

```java
package com.gsim.webui.handlers;

import com.gsim.api.ApiTask;
import com.gsim.api.SseWriter;
import com.gsim.api.TaskManager;
import com.gsim.app.ApplicationContext;
import com.gsim.event.EventBus;
import com.gsim.event.FilteredEventSink;
import com.gsim.event.GSimEvent;
import com.gsim.util.JsonUtils;
import com.sun.net.httpserver.HttpExchange;
import com.sun.net.httpserver.HttpHandler;

import java.io.IOException;
import java.io.OutputStream;
import java.net.URLDecoder;
import java.nio.charset.StandardCharsets;
import java.util.*;

/**
 * 对话 API handler。
 *
 * <p>POST /chat/send     — 发送消息，返回 { taskId, streamUrl }
 * <p>GET  /chat/stream   — SSE 流式事件
 * <p>GET  /chat/messages — 消息历史
 * <p>GET  /chat/context  — 分支上下文
 */
public class ChatHandler implements HttpHandler {

    private final ApplicationContext ctx;

    public ChatHandler(ApplicationContext ctx) {
        this.ctx = ctx;
    }

    @Override
    public void handle(HttpExchange exchange) throws IOException {
        String path = exchange.getRequestURI().getPath();
        String method = exchange.getRequestMethod().toUpperCase();

        try {
            if (path.equals("/chat/send") && "POST".equals(method)) {
                handleSend(exchange);
            } else if (path.equals("/chat/stream") && "GET".equals(method)) {
                handleStream(exchange);
            } else if (path.equals("/chat/messages") && "GET".equals(method)) {
                handleMessages(exchange);
            } else if (path.equals("/chat/context") && "GET".equals(method)) {
                handleContext(exchange);
            } else {
                sendError(exchange, 404, "Unknown chat endpoint");
            }
        } catch (Exception e) {
            sendError(exchange, 500, "Internal error: " + e.getMessage());
        }
    }

    private void handleSend(HttpExchange exchange) throws IOException {
        String body = new String(exchange.getRequestBody().readAllBytes(), StandardCharsets.UTF_8);
        Map<String, Object> req = JsonUtils.fromJson(body, Map.class);
        String message = (String) req.getOrDefault("message", "");
        if (message.isBlank()) {
            sendError(exchange, 400, "message is required");
            return;
        }

        String sessionId = (String) req.getOrDefault("sessionId", "default");

        // 通过 ApiManager 的 TaskManager 创建任务
        TaskManager tm = ctx.getApiManager().getTaskManager();
        String cmd = "/chat " + message;
        ApiTask task = tm.createCommandTask(sessionId, cmd);

        Map<String, Object> result = new LinkedHashMap<>();
        result.put("taskId", task.taskId());
        result.put("streamUrl", "/chat/stream?taskId=" + task.taskId());
        result.put("status", task.status().name());

        sendJson(exchange, 200, result);
    }

    private void handleStream(HttpExchange exchange) throws IOException {
        String query = exchange.getRequestURI().getQuery();
        String taskId = getQueryParam(query, "taskId");
        if (taskId == null || taskId.isBlank()) {
            sendError(exchange, 400, "taskId query param required");
            return;
        }

        TaskManager tm = ctx.getApiManager().getTaskManager();
        if (tm.getTask(taskId) == null) {
            sendError(exchange, 404, "Task not found: " + taskId);
            return;
        }

        SseWriter sse = new SseWriter(exchange);
        sse.sendHeaders();

        // 订阅 EventBus，过滤此 taskId
        EventBus eventBus = ctx.getEventBus();
        FilteredEventSink sink = new FilteredEventSink(
                event -> taskId.equals(event.taskId()),
                event -> {
                    try {
                        Map<String, Object> data = new LinkedHashMap<>();
                        data.put("type", event.type());
                        data.put("taskId", event.taskId());
                        data.put("time", event.time().toString());
                        data.putAll(event.data());
                        sse.writeEvent(event.type(), data);
                    } catch (IOException e) {
                        // SSE 连接已关闭
                    }
                }
        );
        eventBus.subscribe(sink);

        // 保持连接直到 done 事件或超时
        try {
            long deadline = System.currentTimeMillis() + 300_000; // 5 分钟超时
            while (System.currentTimeMillis() < deadline) {
                ApiTask task = tm.getTask(taskId);
                if (task != null && isTerminal(task.status())) {
                    break;
                }
                Thread.sleep(200);
            }
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
        } finally {
            eventBus.unsubscribe(sink);
            sse.close();
        }
    }

    private void handleMessages(HttpExchange exchange) throws IOException {
        // 返回当前分支的消息列表
        var dm = ctx.getDataManager();
        if (dm == null) {
            sendJson(exchange, 200, Map.of("messages", List.of()));
            return;
        }
        // 简化：返回空列表，实际消息在 Phase 2.2 的模板中通过 hx-get 加载
        sendJson(exchange, 200, Map.of("messages", List.of(), "branchId",
                dm.getActiveBranch() != null ? dm.getActiveBranch() : ""));
    }

    private void handleContext(HttpExchange exchange) throws IOException {
        var dm = ctx.getDataManager();
        Map<String, Object> context = new LinkedHashMap<>();
        if (dm != null) {
            context.put("activeBranchId", dm.getActiveBranch());
            context.put("activeWorld", dm.getActiveWorld());
            context.put("activeRootId", dm.getActiveRootId());
        }
        sendJson(exchange, 200, context);
    }

    // ---- helpers ----

    private static void sendJson(HttpExchange exchange, int status, Object data) throws IOException {
        byte[] bytes = JsonUtils.toJsonCompact(data).getBytes(StandardCharsets.UTF_8);
        exchange.getResponseHeaders().set("Content-Type", "application/json; charset=utf-8");
        exchange.getResponseHeaders().set("Access-Control-Allow-Origin", "*");
        exchange.sendResponseHeaders(status, bytes.length);
        try (OutputStream out = exchange.getResponseBody()) {
            out.write(bytes);
        }
    }

    private static void sendError(HttpExchange exchange, int status, String msg) throws IOException {
        sendJson(exchange, status, Map.of("error", msg));
    }

    private static String getQueryParam(String query, String key) {
        if (query == null) return null;
        for (String pair : query.split("&")) {
            int eq = pair.indexOf('=');
            if (eq > 0) {
                String k = URLDecoder.decode(pair.substring(0, eq), StandardCharsets.UTF_8);
                if (key.equals(k)) {
                    return URLDecoder.decode(pair.substring(eq + 1), StandardCharsets.UTF_8);
                }
            }
        }
        return null;
    }

    private static boolean isTerminal(com.gsim.api.ApiTaskStatus status) {
        return status == com.gsim.api.ApiTaskStatus.DONE
                || status == com.gsim.api.ApiTaskStatus.FAILED
                || status == com.gsim.api.ApiTaskStatus.CANCELLED;
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
mvn compile 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
git add src/main/java/com/gsim/webui/handlers/ChatHandler.java
git commit -m "feat: add ChatHandler — send/stream/messages/context endpoints"
```

---

### Task 2.2: 对话面板模板 — SSE 流式消息渲染

**文件:**
- 修改: `src/main/resources/webui/templates/fragments/chat-panel.html`
- 创建: `src/main/resources/webui/templates/fragments/message-bubble.html`

- [ ] **Step 1: 更新 chat-panel.html — 完整对话面板**

```html
<div class="flex flex-col h-full">
    <div class="text-sm font-bold terminal-green mb-2">&#x1F4AC; Agent 对话</div>
    <!-- 分支上下文条 -->
    <div id="chat-context" class="text-xs text-gray-500 mb-2 px-2 py-1 bg-gray-800 rounded"
         hx-get="/chat/context" hx-trigger="load" hx-swap="innerHTML">
        分支: —
    </div>
    <!-- 消息列表 -->
    <div id="chat-messages" class="flex-1 overflow-y-auto mb-2 space-y-2 pr-1">
        <div class="text-gray-600 text-sm">发送消息开始对话</div>
    </div>
    <!-- 输入区 -->
    <form id="chat-form" class="flex gap-2"
          hx-post="/chat/send"
          hx-target="#chat-messages"
          hx-swap="beforeend"
          hx-on::after-request="handleChatResponse(event)">
        <input type="text" name="message" id="chat-input"
               placeholder="输入消息..." autocomplete="off"
               class="flex-1 input-area rounded px-3 py-2 text-sm text-gray-200">
        <button type="submit" id="chat-send-btn"
                class="px-4 py-2 bg-green-700 hover:bg-green-600 text-white text-sm rounded">
            发送
        </button>
    </form>
</div>

<script>
    // 处理发送响应 — 连接 SSE 流
    function handleChatResponse(event) {
        const response = JSON.parse(event.detail.xhr.responseText);
        if (!response.taskId) return;

        // 添加用户消息气泡
        const input = document.getElementById('chat-input');
        const userMsg = input.value;
        input.value = '';
        addMessageBubble('user', userMsg);

        // 添加 assistant 占位气泡
        const asstId = 'msg-' + response.taskId;
        const asstHtml = '<div id="' + asstId + '" class="msg-assistant rounded p-2 text-sm">'
            + '<div class="text-gray-500 text-xs">thinking...</div>'
            + '<div class="msg-content"></div>'
            + '<div class="msg-reasoning hidden text-gray-500 text-xs italic mt-1"></div>'
            + '<div class="msg-tools"></div>'
            + '</div>';
        document.getElementById('chat-messages').insertAdjacentHTML('beforeend', asstHtml);

        // 连接 SSE
        const streamUrl = response.streamUrl;
        const evtSource = new EventSource(streamUrl);
        const msgEl = document.getElementById(asstId);
        const contentEl = msgEl.querySelector('.msg-content');
        const reasoningEl = msgEl.querySelector('.msg-reasoning');
        const toolsEl = msgEl.querySelector('.msg-tools');

        evtSource.addEventListener('llm_delta', function(e) {
            const data = JSON.parse(e.data);
            if (data.content) {
                contentEl.textContent += data.content;
            }
            msgEl.querySelector('.text-gray-500').classList.add('hidden');
        });

        evtSource.addEventListener('llm_reasoning_delta', function(e) {
            const data = JSON.parse(e.data);
            if (data.content) {
                reasoningEl.classList.remove('hidden');
                reasoningEl.textContent += data.content;
            }
        });

        evtSource.addEventListener('tool_started', function(e) {
            const data = JSON.parse(e.data);
            const toolId = 'tool-' + Date.now();
            toolsEl.insertAdjacentHTML('beforeend',
                '<div id="' + toolId + '" class="tool-card rounded p-1 text-xs mt-1">'
                + '<span class="text-yellow-400">&#x2699;</span> '
                + '<span class="tool-name">' + (data.tool || 'tool') + '</span>'
                + ' <span class="text-gray-500">running...</span>'
                + '</div>');
        });

        evtSource.addEventListener('tool_done', function(e) {
            const data = JSON.parse(e.data);
            const cards = toolsEl.querySelectorAll('.tool-card');
            const last = cards[cards.length - 1];
            if (last) {
                last.classList.add('success');
                const statusSpan = last.querySelector('.text-gray-500');
                if (statusSpan) statusSpan.textContent = 'done';
            }
        });

        evtSource.addEventListener('tool_error', function(e) {
            const data = JSON.parse(e.data);
            const cards = toolsEl.querySelectorAll('.tool-card');
            const last = cards[cards.length - 1];
            if (last) {
                last.classList.add('error');
                const statusSpan = last.querySelector('.text-gray-500');
                if (statusSpan) statusSpan.textContent = 'error';
            }
        });

        evtSource.addEventListener('command_done', function(e) {
            evtSource.close();
        });

        evtSource.addEventListener('command_error', function(e) {
            const data = JSON.parse(e.data);
            contentEl.textContent = '[错误] ' + (data.error || 'unknown error');
            evtSource.close();
        });

        evtSource.addEventListener('done', function(e) {
            evtSource.close();
        });

        evtSource.onerror = function(e) {
            evtSource.close();
        };
    }

    function addMessageBubble(role, text) {
        const cls = role === 'user' ? 'msg-user' : 'msg-assistant';
        const html = '<div class="' + cls + ' rounded p-2 text-sm">'
            + '<span class="text-xs text-gray-500">' + (role === 'user' ? 'You' : 'Agent') + '</span>'
            + '<div>' + text + '</div>'
            + '</div>';
        document.getElementById('chat-messages').insertAdjacentHTML('beforeend', html);
    }
</script>
```

- [ ] **Step 2: 编译验证**

```bash
cd /home/cna/GSimulator && mvn compile 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
git add src/main/resources/webui/templates/fragments/chat-panel.html
git commit -m "feat: update chat-panel with SSE streaming message rendering"
```

---

## Phase 2 验收

启动后浏览器访问 `:8711`，在对话面板输入 `/status` 或自然语言，观察：
- 用户消息气泡出现
- Assistant 气泡出现 "thinking..." → 逐 token 流式显示
- Tool call 卡片出现（如有）
- 完成后流停止


---

## Phase 3: 时间线分支面板 — Mermaid + 节点详情

### Task 3.1: MermaidRenderer — 分支数据转 Mermaid 语法

**文件:**
- 创建: `src/main/java/com/gsim/webui/MermaidRenderer.java`

**接口:**
- 消费: `DataManager` (existing)
- 产出: `MermaidRenderer.renderTimeline(dm)` → `Map<String, Object>` 包含 `mermaid` (String), `nodes` (List), `activeBranchId` (String)

- [ ] **Step 1: 创建 MermaidRenderer.java**

```java
package com.gsim.webui;

import com.gsim.data.DataDocument;
import com.gsim.data.DataManager;

import java.util.*;

/**
 * 将分支数据渲染为 Mermaid gitGraph 语法。
 */
public class MermaidRenderer {

    /**
     * 渲染当前 root 的时间线分支图。
     *
     * @return Map with keys: mermaid (String), nodes (List of Map), activeBranchId (String)
     */
    public static Map<String, Object> renderTimeline(DataManager dm) {
        Map<String, Object> result = new LinkedHashMap<>();

        if (dm == null || dm.getActiveBranch() == null) {
            result.put("mermaid", "gitGraph\\n    commit id: \\\"no-data\\\" tag: \\\"无数据\\\"");
            result.put("nodes", List.of());
            result.put("activeBranchId", "");
            return result;
        }

        String activeBranchId = dm.getActiveBranch();
        List<DataDocument> branches = dm.listBranches();
        List<Map<String, Object>> nodes = new ArrayList<>();

        // 构建分支链
        List<DataDocument> chain = dm.getBranchChain(activeBranchId);

        StringBuilder sb = new StringBuilder("gitGraph\n");

        // 从根开始渲染
        if (!chain.isEmpty()) {
            DataDocument root = chain.get(0);
            sb.append("    commit id: \"").append(shortId(root.id()))
                    .append("\" tag: \"").append(escapeTag(root.name())).append("\"\n");
            nodes.add(nodeInfo(root, activeBranchId));

            // 渲染父链上的后续节点
            for (int i = 1; i < chain.size(); i++) {
                DataDocument node = chain.get(i);
                sb.append("    commit id: \"").append(shortId(node.id()))
                        .append("\" tag: \"").append(escapeTag(node.name())).append("\"\n");
                nodes.add(nodeInfo(node, activeBranchId));
            }
        }

        // 渲染子分支
        for (DataDocument branch : branches) {
            if (chain.stream().noneMatch(c -> c.id().equals(branch.id()))) {
                // 不在主链上，作为分支渲染
                Map<String, String> fm = branch.frontMatter();
                String parent = fm.getOrDefault("parent", "");
                if (!parent.isBlank()) {
                    sb.append("    branch ").append(shortId(branch.id())).append("\n");
                    sb.append("    checkout ").append(shortId(branch.id())).append("\n");
                    sb.append("    commit id: \"").append(shortId(branch.id()))
                            .append("\" tag: \"").append(escapeTag(branch.name())).append("\"\n");
                    sb.append("    checkout main\n");
                    nodes.add(nodeInfo(branch, activeBranchId));
                }
            }
        }

        result.put("mermaid", sb.toString());
        result.put("nodes", nodes);
        result.put("activeBranchId", activeBranchId);
        return result;
    }

    private static Map<String, Object> nodeInfo(DataDocument doc, String activeBranchId) {
        Map<String, String> fm = doc.frontMatter();
        Map<String, Object> info = new LinkedHashMap<>();
        info.put("id", doc.id());
        info.put("name", doc.name());
        info.put("turn", fm.getOrDefault("turn", "0"));
        info.put("worldTime", fm.getOrDefault("world_time", "?"));
        info.put("status", fm.getOrDefault("status", "active"));
        info.put("isActive", doc.id().equals(activeBranchId));
        info.put("updated", fm.getOrDefault("updated", ""));
        return info;
    }

    private static String shortId(String id) {
        if (id == null) return "?";
        // branch.b0001-contact → b0001
        int dot = id.lastIndexOf('.');
        return dot >= 0 ? id.substring(dot + 1) : id;
    }

    private static String escapeTag(String name) {
        if (name == null || name.isBlank()) return "?";
        // Mermaid 标签中不能有引号
        return name.replace("\"", "'").replace("#", "");
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
mvn compile 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
git add src/main/java/com/gsim/webui/MermaidRenderer.java
git commit -m "feat: add MermaidRenderer — branch data to gitGraph syntax"
```

---

### Task 3.2: TimelineHandler — 时间线 API

**文件:**
- 创建: `src/main/java/com/gsim/webui/handlers/TimelineHandler.java`

**接口:**
- 消费: `ApplicationContext` (existing), `MermaidRenderer` (Task 3.1)
- 产出: `TimelineHandler` — 处理 `/timeline/data` (GET), `/timeline/node` (GET), `/timeline/activate` (POST)

- [ ] **Step 1: 创建 TimelineHandler.java**

```java
package com.gsim.webui.handlers;

import com.gsim.app.ApplicationContext;
import com.gsim.data.DataManager;
import com.gsim.interaction.InteractionResult;
import com.gsim.interaction.InteractionSession;
import com.gsim.util.JsonUtils;
import com.gsim.webui.MermaidRenderer;
import com.gsim.webui.TemplateRenderer;
import com.sun.net.httpserver.HttpExchange;
import com.sun.net.httpserver.HttpHandler;

import java.io.IOException;
import java.io.OutputStream;
import java.net.URLDecoder;
import java.nio.charset.StandardCharsets;
import java.util.*;

/**
 * 时间线 API handler。
 *
 * <p>GET  /timeline/data     — 分支树 JSON + Mermaid 语法
 * <p>GET  /timeline/node     — 节点详情 HTML 片段
 * <p>POST /timeline/activate — 切换活动分支
 */
public class TimelineHandler implements HttpHandler {

    private final ApplicationContext ctx;

    public TimelineHandler(ApplicationContext ctx) {
        this.ctx = ctx;
    }

    @Override
    public void handle(HttpExchange exchange) throws IOException {
        String path = exchange.getRequestURI().getPath();
        String method = exchange.getRequestMethod().toUpperCase();

        try {
            if (path.equals("/timeline/data") && "GET".equals(method)) {
                handleData(exchange);
            } else if (path.equals("/timeline/node") && "GET".equals(method)) {
                handleNode(exchange);
            } else if (path.equals("/timeline/activate") && "POST".equals(method)) {
                handleActivate(exchange);
            } else {
                sendError(exchange, 404, "Unknown timeline endpoint");
            }
        } catch (Exception e) {
            sendError(exchange, 500, "Internal error: " + e.getMessage());
        }
    }

    private void handleData(HttpExchange exchange) throws IOException {
        DataManager dm = ctx.getDataManager();
        Map<String, Object> data = MermaidRenderer.renderTimeline(dm);
        sendJson(exchange, 200, data);
    }

    private void handleNode(HttpExchange exchange) throws IOException {
        String query = exchange.getRequestURI().getQuery();
        String nodeId = getQueryParam(query, "id");
        if (nodeId == null || nodeId.isBlank()) {
            sendError(exchange, 400, "id query param required");
            return;
        }

        DataManager dm = ctx.getDataManager();
        if (dm == null) {
            sendError(exchange, 500, "DataManager not available");
            return;
        }

        var doc = dm.readById(nodeId);
        if (doc == null) {
            sendError(exchange, 404, "Node not found: " + nodeId);
            return;
        }

        Map<String, String> fm = doc.frontMatter();
        Map<String, Object> vars = new LinkedHashMap<>();
        vars.put("id", doc.id());
        vars.put("name", doc.name());
        vars.put("turn", fm.getOrDefault("turn", "0"));
        vars.put("worldTime", fm.getOrDefault("world_time", "?"));
        vars.put("status", fm.getOrDefault("status", "active"));
        vars.put("updated", fm.getOrDefault("updated", ""));
        vars.put("isActive", doc.id().equals(dm.getActiveBranch()));

        String html = TemplateRenderer.render("fragments/node-detail", vars);
        sendHtml(exchange, 200, html);
    }

    private void handleActivate(HttpExchange exchange) throws IOException {
        String body = new String(exchange.getRequestBody().readAllBytes(), StandardCharsets.UTF_8);
        Map<String, Object> req = JsonUtils.fromJson(body, Map.class);
        String branchId = (String) req.get("branchId");
        if (branchId == null || branchId.isBlank()) {
            sendError(exchange, 400, "branchId is required");
            return;
        }

        InteractionSession session = ctx.getSessionManager() != null
                ? ctx.getSessionManager().getOrCreateSession("default")
                : null;
        if (session == null) {
            sendError(exchange, 500, "Session not available");
            return;
        }

        InteractionResult result = ctx.getInteractionManager()
                .handle("/data branch switch " + branchId, session);

        Map<String, Object> resp = new LinkedHashMap<>();
        resp.put("success", result.success());
        resp.put("message", result.displayText());
        resp.put("branchId", branchId);
        sendJson(exchange, result.success() ? 200 : 400, resp);
    }

    // ---- helpers ----

    private static void sendJson(HttpExchange exchange, int status, Object data) throws IOException {
        byte[] bytes = JsonUtils.toJsonCompact(data).getBytes(StandardCharsets.UTF_8);
        exchange.getResponseHeaders().set("Content-Type", "application/json; charset=utf-8");
        exchange.getResponseHeaders().set("Access-Control-Allow-Origin", "*");
        exchange.sendResponseHeaders(status, bytes.length);
        try (OutputStream out = exchange.getResponseBody()) {
            out.write(bytes);
        }
    }

    private static void sendHtml(HttpExchange exchange, int status, String html) throws IOException {
        byte[] bytes = html.getBytes(StandardCharsets.UTF_8);
        exchange.getResponseHeaders().set("Content-Type", "text/html; charset=utf-8");
        exchange.sendResponseHeaders(status, bytes.length);
        try (OutputStream out = exchange.getResponseBody()) {
            out.write(bytes);
        }
    }

    private static void sendError(HttpExchange exchange, int status, String msg) throws IOException {
        sendJson(exchange, status, Map.of("error", msg));
    }

    private static String getQueryParam(String query, String key) {
        if (query == null) return null;
        for (String pair : query.split("&")) {
            int eq = pair.indexOf('=');
            if (eq > 0) {
                String k = URLDecoder.decode(pair.substring(0, eq), StandardCharsets.UTF_8);
                if (key.equals(k)) {
                    return URLDecoder.decode(pair.substring(eq + 1), StandardCharsets.UTF_8);
                }
            }
        }
        return null;
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
mvn compile 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
git add src/main/java/com/gsim/webui/handlers/TimelineHandler.java
git commit -m "feat: add TimelineHandler — data/node/activate endpoints"
```

---

### Task 3.3: 时间线面板模板 — Mermaid 渲染 + 节点交互

**文件:**
- 修改: `src/main/resources/webui/templates/fragments/timeline-panel.html`
- 创建: `src/main/resources/webui/templates/fragments/node-detail.html`

- [ ] **Step 1: 更新 timeline-panel.html — 完整时间线面板**

```html
<div>
    <div class="text-sm font-bold terminal-green mb-2">&#x1F333; 时间线分支</div>

    <!-- Mermaid 图表容器 -->
    <div id="timeline-graph" class="mb-3 p-2 bg-gray-900 rounded overflow-x-auto"
         hx-get="/timeline/data" hx-trigger="load" hx-swap="none"
         hx-on::after-request="loadTimeline(event)">
        <div class="text-gray-600 text-xs">加载时间线...</div>
    </div>

    <!-- 节点详情面板 -->
    <div id="timeline-detail" class="text-xs text-gray-500 mb-3">
        <div class="text-gray-600">点击节点查看详情</div>
    </div>

    <!-- 节点列表 -->
    <div id="timeline-nodes" class="text-xs">
        <div class="text-gray-500 font-bold mb-1">分支节点</div>
        <div id="timeline-node-list" class="space-y-1">
        </div>
    </div>

    <!-- 刷新按钮 -->
    <button class="mt-3 px-3 py-1 bg-gray-700 hover:bg-gray-600 text-gray-300 text-xs rounded"
            hx-get="/timeline" hx-target="closest div" hx-swap="outerHTML">
        &#x21BB; 刷新
    </button>
</div>

<script>
    function loadTimeline(event) {
        const data = JSON.parse(event.detail.xhr.responseText);
        if (!data.mermaid) return;

        // 渲染 Mermaid
        const graphEl = document.getElementById('timeline-graph');
        graphEl.innerHTML = '<div class="mermaid">' + data.mermaid + '</div>';
        mermaid.run({ querySelector: '.mermaid' });

        // 渲染节点列表
        const nodeList = document.getElementById('timeline-node-list');
        nodeList.innerHTML = '';
        if (data.nodes) {
            data.nodes.forEach(function(node) {
                const activeClass = node.isActive
                    ? 'border-green-400 text-green-400'
                    : 'border-gray-600 text-gray-400 hover:border-gray-400';
                const html = '<div class="border-l-2 ' + activeClass + ' pl-2 py-1 cursor-pointer"'
                    + ' hx-get="/timeline/node?id=' + node.id + '"'
                    + ' hx-target="#timeline-detail" hx-swap="innerHTML">'
                    + '<div class="font-bold">' + node.name + '</div>'
                    + '<div class="text-gray-600">Turn ' + node.turn
                    + ' | ' + node.worldTime + '</div>'
                    + '</div>';
                nodeList.insertAdjacentHTML('beforeend', html);
                // 重新处理 HTMX 属性
                htmx.process(nodeList.lastElementChild);
            });
        }
    }
</script>
```

- [ ] **Step 2: 创建 node-detail.html 片段**

```html
<div class="p-2 bg-gray-800 rounded" th:if="${id != null}">
    <div class="font-bold terminal-green" th:text="${name}">Node Name</div>
    <div class="mt-1 space-y-1">
        <div>ID: <span class="text-gray-400" th:text="${id}">—</span></div>
        <div>Turn: <span class="text-gray-400" th:text="${turn}">0</span></div>
        <div>世界时间: <span class="text-gray-400" th:text="${worldTime}">?</span></div>
        <div>状态: <span class="text-gray-400" th:text="${status}">active</span></div>
        <div>更新: <span class="text-gray-400" th:text="${updated}">—</span></div>
    </div>
    <div class="mt-2 flex gap-2" th:if="${!isActive}">
        <button class="px-2 py-1 bg-green-700 hover:bg-green-600 text-white text-xs rounded"
                hx-post="/timeline/activate"
                hx-vals='{"branchId":"[[${id}]]"}'
                hx-swap="none"
                hx-on::after-request="location.reload()">
            切换到此分支
        </button>
    </div>
    <div class="text-xs text-green-400 mt-1" th:if="${isActive}">&#x2714; 当前活动分支</div>
</div>
<div class="text-gray-600 text-xs" th:if="${id == null}">
    点击节点查看详情
</div>
```

- [ ] **Step 3: 编译验证**

```bash
mvn compile 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add src/main/resources/webui/templates/fragments/timeline-panel.html \
        src/main/resources/webui/templates/fragments/node-detail.html
git commit -m "feat: update timeline panel with Mermaid rendering and node interaction"
```

---

## Phase 3 验收

浏览器访问 `:8711`，时间线面板：
- 加载后显示 Mermaid gitGraph
- 节点列表显示所有分支
- 点击节点显示详情
- 点击"切换到此分支"后页面刷新，新分支高亮


---

## Phase 4: 知识库搜索面板 — keyword + semantic

### Task 4.1: KnowledgeHandler — 知识库搜索 API

**文件:**
- 创建: `src/main/java/com/gsim/webui/handlers/KnowledgeHandler.java`

**接口:**
- 消费: `ApplicationContext` (existing), `KnowledgeSearchService` (existing)
- 产出: `KnowledgeHandler` — 处理 `/knowledge/search` (GET/POST), `/knowledge/detail` (GET)

- [ ] **Step 1: 创建 KnowledgeHandler.java**

```java
package com.gsim.webui.handlers;

import com.gsim.app.ApplicationContext;
import com.gsim.knowledge.KnowledgeSearchResponse;
import com.gsim.knowledge.KnowledgeSearchResult;
import com.gsim.knowledge.search.KnowledgeSearchService;
import com.gsim.knowledge.store.KnowledgeStore;
import com.gsim.util.JsonUtils;
import com.gsim.webui.TemplateRenderer;
import com.sun.net.httpserver.HttpExchange;
import com.sun.net.httpserver.HttpHandler;

import java.io.IOException;
import java.io.OutputStream;
import java.net.URLDecoder;
import java.nio.charset.StandardCharsets;
import java.util.*;

/**
 * 知识库搜索 API handler。
 *
 * <p>GET/POST /knowledge/search — keyword 或 semantic 搜索
 * <p>GET     /knowledge/detail — 结果详情
 */
public class KnowledgeHandler implements HttpHandler {

    private final ApplicationContext ctx;

    public KnowledgeHandler(ApplicationContext ctx) {
        this.ctx = ctx;
    }

    @Override
    public void handle(HttpExchange exchange) throws IOException {
        String path = exchange.getRequestURI().getPath();
        String method = exchange.getRequestMethod().toUpperCase();

        try {
            if (path.equals("/knowledge/search")) {
                if ("GET".equals(method)) handleSearchGet(exchange);
                else if ("POST".equals(method)) handleSearchPost(exchange);
                else sendError(exchange, 405, "Method not allowed");
            } else if (path.equals("/knowledge/detail") && "GET".equals(method)) {
                handleDetail(exchange);
            } else {
                sendError(exchange, 404, "Unknown knowledge endpoint");
            }
        } catch (Exception e) {
            sendError(exchange, 500, "Search failed: " + e.getMessage());
        }
    }

    private void handleSearchGet(HttpExchange exchange) throws IOException {
        String query = exchange.getRequestURI().getQuery();
        String q = getQueryParam(query, "q");
        String mode = getQueryParam(query, "mode");
        String topKStr = getQueryParam(query, "topK");
        if (q == null || q.isBlank()) {
            sendError(exchange, 400, "q query param required");
            return;
        }
        if (mode == null) mode = "keyword";
        int topK = 10;
        try { if (topKStr != null) topK = Integer.parseInt(topKStr); } catch (NumberFormatException ignored) {}

        executeSearch(exchange, q, mode, topK);
    }

    private void handleSearchPost(HttpExchange exchange) throws IOException {
        String body = new String(exchange.getRequestBody().readAllBytes(), StandardCharsets.UTF_8);
        Map<String, Object> req = JsonUtils.fromJson(body, Map.class);
        String q = (String) req.getOrDefault("q", "");
        String mode = (String) req.getOrDefault("mode", "keyword");
        int topK = req.containsKey("topK") ? ((Number) req.get("topK")).intValue() : 10;
        if (q.isBlank()) {
            sendError(exchange, 400, "q is required");
            return;
        }
        executeSearch(exchange, q, mode, topK);
    }

    private void executeSearch(HttpExchange exchange, String q, String mode, int topK) throws IOException {
        List<Map<String, Object>> results = new ArrayList<>();

        if ("semantic".equals(mode)) {
            KnowledgeSearchService ss = ctx.getKnowledgeSearchService();
            if (ss == null) {
                sendError(exchange, 400, "Semantic search not available: no embedding profile configured. Use keyword mode.");
                return;
            }
            KnowledgeSearchResponse resp = ss.semanticSearch(q, "default", topK);
            if (resp.success() && resp.items() != null) {
                for (KnowledgeSearchResult r : resp.items()) {
                    results.add(resultToMap(r));
                }
            } else if (resp.error() != null) {
                sendError(exchange, 400, resp.error().message());
                return;
            }
        } else {
            // keyword search
            var store = ctx.getKnowledgeStore();
            if (store == null) {
                results = List.of();
            } else {
                List<KnowledgeSearchResult> kwResults = store.searchKeyword(q, "default", topK);
                for (KnowledgeSearchResult r : kwResults) {
                    results.add(resultToMap(r));
                }
            }
        }

        String html = renderResults(q, mode, results);
        sendHtml(exchange, 200, html);
    }

    private Map<String, Object> resultToMap(KnowledgeSearchResult r) {
        Map<String, Object> m = new LinkedHashMap<>();
        m.put("chunkId", r.chunkId());
        m.put("docId", r.docId());
        m.put("title", r.title());
        m.put("snippet", r.snippet());
        m.put("score", String.format("%.3f", r.finalScore()));
        m.put("sourceUri", r.sourceUri() != null ? r.sourceUri() : "");
        m.put("collection", r.collection());
        return m;
    }

    private String renderResults(String q, String mode, List<Map<String, Object>> results) {
        Map<String, Object> vars = new LinkedHashMap<>();
        vars.put("query", q);
        vars.put("mode", mode);
        vars.put("count", results.size());
        vars.put("results", results);
        return TemplateRenderer.render("fragments/search-results", vars);
    }

    private void handleDetail(HttpExchange exchange) throws IOException {
        String query = exchange.getRequestURI().getQuery();
        String chunkId = getQueryParam(query, "id");
        if (chunkId == null || chunkId.isBlank()) {
            sendError(exchange, 400, "id query param required");
            return;
        }

        var store = ctx.getKnowledgeStore();
        if (store == null) {
            sendError(exchange, 500, "Knowledge store not available");
            return;
        }

        var chunk = store.getChunk(chunkId);
        if (chunk.isEmpty()) {
            sendError(exchange, 404, "Chunk not found: " + chunkId);
            return;
        }

        Map<String, Object> vars = new LinkedHashMap<>();
        vars.put("chunkId", chunkId);
        vars.put("title", chunk.get().title());
        vars.put("text", chunk.get().text());
        vars.put("docId", chunk.get().docId());
        vars.put("collection", chunk.get().collection());

        String html = TemplateRenderer.render("fragments/search-detail", vars);
        sendHtml(exchange, 200, html);
    }

    // ---- helpers ----

    private static void sendJson(HttpExchange exchange, int status, Object data) throws IOException {
        byte[] bytes = JsonUtils.toJsonCompact(data).getBytes(StandardCharsets.UTF_8);
        exchange.getResponseHeaders().set("Content-Type", "application/json; charset=utf-8");
        exchange.getResponseHeaders().set("Access-Control-Allow-Origin", "*");
        exchange.sendResponseHeaders(status, bytes.length);
        try (OutputStream out = exchange.getResponseBody()) {
            out.write(bytes);
        }
    }

    private static void sendHtml(HttpExchange exchange, int status, String html) throws IOException {
        byte[] bytes = html.getBytes(StandardCharsets.UTF_8);
        exchange.getResponseHeaders().set("Content-Type", "text/html; charset=utf-8");
        exchange.sendResponseHeaders(status, bytes.length);
        try (OutputStream out = exchange.getResponseBody()) {
            out.write(bytes);
        }
    }

    private static void sendError(HttpExchange exchange, int status, String msg) throws IOException {
        sendJson(exchange, status, Map.of("error", msg));
    }

    private static String getQueryParam(String query, String key) {
        if (query == null) return null;
        for (String pair : query.split("&")) {
            int eq = pair.indexOf('=');
            if (eq > 0) {
                String k = URLDecoder.decode(pair.substring(0, eq), StandardCharsets.UTF_8);
                if (key.equals(k)) {
                    return URLDecoder.decode(pair.substring(eq + 1), StandardCharsets.UTF_8);
                }
            }
        }
        return null;
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
mvn compile 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
git add src/main/java/com/gsim/webui/handlers/KnowledgeHandler.java
git commit -m "feat: add KnowledgeHandler — keyword/semantic search + detail endpoints"
```

---

### Task 4.2: 知识库面板模板 — 搜索结果渲染

**文件:**
- 修改: `src/main/resources/webui/templates/fragments/knowledge-panel.html`
- 创建: `src/main/resources/webui/templates/fragments/search-results.html`
- 创建: `src/main/resources/webui/templates/fragments/search-detail.html`

- [ ] **Step 1: 更新 knowledge-panel.html**

```html
<div>
    <div class="text-sm font-bold terminal-green mb-2">&#x1F50D; 知识库</div>
    <form id="search-form" class="mb-3"
          hx-get="/knowledge/search"
          hx-target="#search-results"
          hx-swap="innerHTML"
          hx-indicator="#search-spinner">
        <div class="flex gap-1 mb-2">
            <input type="text" name="q" placeholder="搜索关键词..."
                   class="flex-1 input-area rounded px-3 py-1 text-sm text-gray-200">
            <button type="submit"
                    class="px-3 py-1 bg-green-700 hover:bg-green-600 text-white text-sm rounded">
                搜索
            </button>
        </div>
        <div class="flex gap-2 text-xs items-center">
            <label class="text-gray-400">
                <input type="radio" name="mode" value="keyword" checked> 关键词
            </label>
            <label class="text-gray-400">
                <input type="radio" name="mode" value="semantic"> 语义
            </label>
            <input type="hidden" name="topK" value="10">
            <span id="search-spinner" class="htmx-indicator text-green-400">...</span>
        </div>
    </form>
    <div id="search-results" class="text-xs text-gray-500">
        <div class="text-gray-600">输入关键词搜索知识库</div>
    </div>
</div>
```

- [ ] **Step 2: 创建 search-results.html**

```html
<div>
    <div class="text-gray-400 mb-1">
        搜索 "<span th:text="${query}">q</span>"
        (<span th:text="${mode}">keyword</span>)
        — <span th:text="${count}">0</span> 条结果
    </div>
    <div class="space-y-2" th:if="${count > 0}">
        <div th:each="r : ${results}" class="p-2 bg-gray-800 rounded hover:bg-gray-700 cursor-pointer"
             hx-get="'/knowledge/detail?id=' + ${r.chunkId}"
             hx-target="#search-detail-panel" hx-swap="innerHTML">
            <div class="font-bold text-gray-300" th:text="${r.title}">Title</div>
            <div class="text-gray-500 mt-1" th:text="${r.snippet}">Snippet</div>
            <div class="flex justify-between mt-1">
                <span class="text-gray-600" th:text="${r.collection}">collection</span>
                <span class="text-green-400" th:text="${r.score}">0.0</span>
            </div>
        </div>
    </div>
    <div class="text-gray-600" th:if="${count == 0}">
        无结果
    </div>
    <!-- 详情弹出区 -->
    <div id="search-detail-panel" class="mt-2"></div>
</div>
```

- [ ] **Step 3: 创建 search-detail.html**

```html
<div class="mt-2 p-3 bg-gray-800 rounded border border-gray-600">
    <div class="font-bold terminal-green text-sm" th:text="${title}">Title</div>
    <div class="text-xs text-gray-500 mt-1">
        Chunk: <span th:text="${chunkId}">—</span>
        | Doc: <span th:text="${docId}">—</span>
        | Collection: <span th:text="${collection}">—</span>
    </div>
    <div class="mt-2 text-xs text-gray-300 whitespace-pre-wrap max-h-60 overflow-y-auto"
         th:text="${text}">Content</div>
    <button class="mt-2 text-xs text-gray-500 hover:text-gray-300"
            onclick="this.parentElement.remove()">&times; 关闭</button>
</div>
```

- [ ] **Step 4: 编译验证**

```bash
mvn compile 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 5: Commit**

```bash
git add src/main/resources/webui/templates/fragments/knowledge-panel.html \
        src/main/resources/webui/templates/fragments/search-results.html \
        src/main/resources/webui/templates/fragments/search-detail.html
git commit -m "feat: update knowledge panel with search results and detail rendering"
```

---

## Phase 4 验收

浏览器访问 `:8711`，知识库面板：
- 输入关键词，选择 keyword/semantic 模式
- 点击搜索，结果列表更新
- 点击结果条目，显示详情


---

## Phase 5: 移动端适配 + 收尾

### Task 5.1: 移动端响应式完善

**文件:**
- 修改: `src/main/resources/webui/static/app.js`
- 修改: `src/main/resources/webui/templates/index.html`

- [ ] **Step 1: 完善 app.js — 添加移动端面板加载策略**

在 `app.js` 末尾添加：

```javascript
// 移动端面板延迟加载策略
(function() {
    if (window.innerWidth >= 768) return; // 桌面端跳过

    // 移动端：聊天面板默认激活已由 index.html 的 hx-trigger="load" 处理
    // 其他面板在切换时才首次加载
    let loaded = { chat: true, timeline: false, knowledge: false };

    // 重写 switchMobilePanel 处理延迟加载
    const origSwitch = switchMobilePanel;
    switchMobilePanel = function(name) {
        if (!loaded[name]) {
            loaded[name] = true;
            const el = document.getElementById('mobile-' + name);
            if (el) {
                const child = el.querySelector('[hx-get]');
                if (child) htmx.trigger(child, 'load');
            }
        }
        origSwitch(name);
    };
})();
```

- [ ] **Step 2: 在 index.html 中确保移动端默认显示聊天面板且有内容**

index.html 中移动端部分已有正确的 `hx-trigger="load"`。确认无需额外修改。

- [ ] **Step 3: 编译验证**

```bash
mvn compile 2>&1 | tail -5
```

Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
git add src/main/resources/webui/static/app.js
git commit -m "feat: add mobile lazy-loading for panel switching"
```

---

### Task 5.2: 辅助静态资源 + favicon

**文件:**
- 创建: `src/main/resources/webui/static/favicon.svg`（可选，不影响功能）

- [ ] **Step 1: 创建简单的 SVG favicon**

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="4" fill="#111827"/>
  <text x="16" y="22" text-anchor="middle" fill="#34d399" font-size="20" font-family="monospace" font-weight="bold">GS</text>
</svg>
```

- [ ] **Step 2: Commit**

```bash
git add src/main/resources/webui/static/favicon.svg
git commit -m "feat: add SVG favicon for WebUI"
```

---

## Phase 6: 集成测试 + 最终验收

### Task 6.1: 确保 mvn test 全通过

- [ ] **Step 1: 运行全量测试**

```bash
mvn test 2>&1 | tail -20
```

Expected: BUILD SUCCESS, 所有已有测试通过

- [ ] **Step 2: 如有测试失败，修复**

检查是否新增代码影响了现有测试。

- [ ] **Step 3: 运行 mvn package 确认 fat jar 可构建**

```bash
mvn package -DskipTests 2>&1 | tail -10
```

Expected: BUILD SUCCESS

---

### Task 6.2: 手动验收清单

启动命令：
```bash
java -jar target/GSimulator.jar --http --webui --no-wizard
```

#### 验收项：

1. **启动输出**
   - [ ] 控制台显示 `🌐 HTTP API: http://127.0.0.1:8710`
   - [ ] 控制台显示 `🌐 Web GUI: http://127.0.0.1:8711`

2. **主页面** (浏览器访问 `http://127.0.0.1:8711`)
   - [ ] 页面加载成功，标题 "GSimulator — 推演工作台"
   - [ ] 桌面端 (≥768px)：三栏布局，左时间线 + 中对话 + 右知识库
   - [ ] 移动端 (<768px)：单面板 + 汉堡菜单

3. **对话面板**
   - [ ] 输入框和发送按钮可见
   - [ ] 输入 `/status` 发送
   - [ ] 用户消息气泡出现
   - [ ] Assistant 流式回复出现
   - [ ] 完成后流停止，发送按钮恢复

4. **时间线面板**
   - [ ] Mermaid 图表加载
   - [ ] 节点列表显示
   - [ ] 点击节点显示详情
   - [ ] "切换到此分支"按钮出现（非活动分支）

5. **知识库面板**
   - [ ] 搜索框可见
   - [ ] keyword/semantic 模式切换
   - [ ] 输入关键词搜索，结果列表更新
   - [ ] 点击结果，详情出现

6. **移动端**
   - [ ] 汉堡菜单点击打开/关闭侧边栏
   - [ ] 侧边栏三个模块切换正常
   - [ ] 各面板内容正确加载

---

### Task 6.3: 最终 Commit

- [ ] **Step 1: 提交所有剩余变更**

```bash
git add -A
git commit -m "feat: complete Web GUI — Phase 1-6 implementation

- HTMX + Thymeleaf + SSE + Mermaid 技术栈
- 三大模块：Agent 对话（流式）、时间线分支、知识库搜索
- 桌面三栏 + 移动端侧边栏响应式布局
- --webui CLI 参数，独立端口 8711
- 暗色主题，零 npm/Node 依赖"
```

---

## Phase 6 验收完成标准

- `mvn test` BUILD SUCCESS
- `mvn package` BUILD SUCCESS
- 手动验收清单 6 项全部通过
- 浏览器访问 `http://127.0.0.1:8711` 三大模块功能正常
