<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.gsim</groupId>
        <artifactId>GSimulator</artifactId>
        <version>0.1.0-Alpha260723</version>
    </parent>

    <artifactId>gsim-app</artifactId>
    <packaging>jar</packaging>

    <name>GSimulator App</name>
    <description>GSimulator 程序入口 + 应用组装 + 交互壳（CLI/WebUI）</description>

    <dependencies>
        <dependency>
            <groupId>com.gsim</groupId>
            <artifactId>gsim-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.gsim</groupId>
            <artifactId>gsim-agent</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.gsim</groupId>
            <artifactId>gsim-map</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.gsim</groupId>
            <artifactId>gsim-agentsmanager</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.gsim</groupId>
            <artifactId>gsim-docslib</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 交互壳直接依赖（app/commands/interaction/webui 迁入后不再经 core 传递） -->
        <dependency>
            <groupId>org.jline</groupId>
            <artifactId>jline</artifactId>
        </dependency>
        <dependency>
            <groupId>org.thymeleaf</groupId>
            <artifactId>thymeleaf</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-annotations</artifactId>
            <version>4.10.2</version>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- SpotBugs: ratchet 模式 — baseline 文件 config/spotbugs/gsim-app-baseline.xml（2026-08-13 重建，70 个 issue）；
                 app 模块承载迁移代码（GSimulatorApplication 等），与 core/agent 一致的 baseline 对比模式（failOnError=false）；
                 零容忍（failOnError=true）留给 gsim-agentsmanager/gsim-map。 -->
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <configuration>
                    <effort>Max</effort>
                    <threshold>Medium</threshold>
                    <failOnError>false</failOnError>
                    <includeTests>false</includeTests>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <phase>package</phase>
                        <configuration>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.gsim.Main</mainClass>
                                </transformer>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                            </transformers>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
